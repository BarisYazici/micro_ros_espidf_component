{
    "name": "Micro-ROS ESP-IDF",
    "image": "barisyazici747/microros-esp-idf-v5.3-dev-1162-gee3a46dbc3",
    "forwardPorts": [
        8080
    ],
    "settings": {
        "terminal.integrated.shell.linux": "/bin/bash"
    },
    "mounts": [
        "source=/etc/timezone,target=/etc/timezone,type=bind,consistency=cached,readonly",
        "source=../,target=/micro_ros_espidf_component,type=bind,consistency=cached",
        "source=/dev,target=/dev,type=bind,consistency=cached"
    ],
    "runArgs": [
        "--privileged",
        "--net=host"
    ],
    "remoteEnv": {
        "ROS_DOMAIN_ID": "100"
    },
    "workspaceFolder": "/micro_ros_espidf_component",
    "postCreateCommand": "echo 'Devcontainer for Micro-ROS ESP-IDF is ready!'"
}